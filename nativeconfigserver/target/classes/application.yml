#本地配置
server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
    #类型
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          #配置文件存放路径
          search-locations: classpath:/shared